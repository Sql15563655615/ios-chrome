# iOS UIKit Shell

The UIKit shell will be Objective-C++ and C++ only. It must not use Swift and must not use `WKWebView` as the page renderer.

Planned files:

- `AppDelegate.mm`
- `BrowserViewController.mm`
- `ChromiumView.mm`
- `TouchHandler.mm`
- `KeyboardHandler.mm`
